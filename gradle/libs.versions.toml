[versions]
cvssCalculator = "1.4.1"
junit = "4.13.2"

[libraries]
cvss-calculator = { module = "us.springett:cvss-calculator", version.ref = "cvssCalculator" }
junit = { module = "junit:junit", version.ref = "junit" }
